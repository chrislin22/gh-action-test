name: nested matrix
on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        ddata: [ [1, 2], [a, b] ]
    
    steps:
      - name: check matrix content ${{ matrix.ddata }}
        run: |
          echo ${{ matrix.ddata }}

