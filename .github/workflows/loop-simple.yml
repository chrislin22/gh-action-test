name: loop-simple

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - run: | 
          echo "aaa=XYZ" >> ${GITHUB_ENV}
          
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: 
          - a
          - b
          - c
    steps:
      - run: | 
          echo ${{ matrix.node }}
          echo ${aaa}
